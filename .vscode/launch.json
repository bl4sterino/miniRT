{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: Run with Args (make debug)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/miniRT_debug",
            // "args": ["Maps/42.rt"],
            // "args": ["Maps/cubeFDF.rt"],
            //"args": ["Maps/test_scene"],
            //"args": ["Maps/fog.rt"],
            // "args": ["Maps/specularity.rt"],
            // "args": ["Maps/saturn.rt"],
            // "args": ["Maps/triangles.rt"],
            // "args": ["Maps/box.rt"],
            "args": ["Maps/test_scene.rt"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            // This is the crucial line: it runs the task defined in tasks.json before starting the debugger.
            "preLaunchTask": "make-debug"
        }
    ]
}